var dan;          // this is a declaration
dan = 'the man';  // this is an assignment

var spencer = 43; // this is both assignment and declaration in a single statements

var dylan, corey, uma, barakat;  // multiple declarations

var megan,
    jp,
    jed,
    bob;

var dan;
var nadia;
var spencer;
